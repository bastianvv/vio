meta {
  name: full-scan series
  type: http
  seq: 3
}

post {
  url: {{base_url}}{{api_path}}{{libraries_path}}/2/rescan
  body: none
  auth: inherit
}

tests {
  test("Should return 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should return scan job running", function () {  
    expect(res.body.job_id).to.not.be.null;
    expect(res.body.status).to.be.equals("running")
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
