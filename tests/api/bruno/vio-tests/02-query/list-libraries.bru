meta {
  name: list-libraries
  type: http
  seq: 1
}

get {
  url: {{base_url}}{{api_path}}{{libraries_path}}
  body: none
  auth: inherit
}

tests {
  test("Return 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Libraries list returned", () => {
    expect(Array.isArray(res.body)).to.be.true;
    expect(res.body.length).to.be.greaterThan(0);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
