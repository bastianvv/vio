meta {
  name: create-series-library
  type: http
  seq: 2
}

post {
  url: {{base_url}}{{api_path}}{{libraries_path}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "name": "My TV Series",
      "type": "series",
      "path": "/mnt/Media/vio-test/tv"
  }
}

tests {
  test("Return 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Series library created", () => {
    expect(res.status).to.equal(201);
    expect(res.body.id).to.be.a("number");
    expect(res.body.type).to.equal("series");
  });
  
  bru.setEnvVar("series_library_id", res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
