meta {
  name: create-movies-library
  type: http
  seq: 1
}

post {
  url: {{base_url}}{{api_path}}{{libraries_path}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
      "name": "My Movies",
      "type": "movies",
      "path": "/mnt/Media/vio-test/movies"
  }
}

tests {
  test("Return 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
  
  test("Movies library created", () => {
    expect(res.status).to.equal(201);
    expect(res.body.id).to.be.a("number");
    expect(res.body.type).to.equal("movies");
  });
  
  bru.setEnvVar("movies_library_id", res.body.id);
}

settings {
  encodeUrl: true
  timeout: 0
}
